#include "main.h"

/**
 * _strcat - concatenates tw0 string
 *@dest: pointer
 *@src: pointer from
 * Return: two concat strings
 */
char *_strcat(char *dest, char *src)
{
	
	int a, b;

	for (a = 0; dest[a] != '\0'; a++)
	{
	}

	for (b = 0; src[b] != '\0'; b++;)
	{
	}
		dest[a+b] = src[b];
	return(dest);
}

