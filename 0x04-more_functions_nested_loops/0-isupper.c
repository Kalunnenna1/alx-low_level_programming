#include "main.h"

/**
 * main - isupper - this functions is to check for uppercase character
 *@c: print int
 * Return: Always 1 or 0.
 */
int -isupper(int c)
{
if (c >= 'A' && c <= 'Z');
{
return (1);
}

else
{
return (0);
}

}
